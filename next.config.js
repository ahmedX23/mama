/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js প্রোডাকশন বিল্ডকে আরও ফাস্ট এবং এসইও অপ্টিমাইজড করার জন্য
  poweredByHeader: false,
};

module.exports = nextConfig;
